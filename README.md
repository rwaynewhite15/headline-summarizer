# Headline Summarizer

A simple GitHub Pages-deployable app that retrieves headlines from specified sources and generates AI summaries of current events. Easily integrate with APIs like OpenAI and NewsAPI.